import 'package:flutter/material.dart';

const titleText = TextStyle(
  color: Colors.white,
  fontWeight: FontWeight.bold,
  fontSize: 18,
);

const roundedBox = BoxDecoration(
  color: Colors.white,
  borderRadius: BorderRadius.all(Radius.circular(20.0)),
);
